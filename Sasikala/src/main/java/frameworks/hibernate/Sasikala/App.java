package frameworks.hibernate.Sasikala;

import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;

/**
 * Hello world!
 *
 */
public class App 
{
    public static void main( String[] args )
    {
    	/*SessionFactory factory=new Configuration().configure().buildSessionFactory();
    	Session session=factory.openSession();
    	session.beginTransaction();
    	
    	Manufact m1=new Manufact("Relatek","Autralia",3);
    	Manufact m2=new Manufact("Samsung","Korea",4);
    	Manufact m3=new Manufact("Sandisk","UK",4);
    	Manufact m4=new Manufact("Snapdragon","Japan",2);
    	
    	
    	Tabs t1=new Tabs("Redmi", 10, 7200.3);
    	t1.getMulti().add(m1);t1.getMulti().add(m2);
    	t1.getMulti().add(m3);t1.getMulti().add(m4);
    	t1.setId(3);
    	
    	session.save(t1);
    	
    	session.getTransaction().commit();
    	session.close();*/
    }
}
